import { alerts, neutral, primary, secundary } from "./colors";
import { buttonsTheme } from "./buttons";

export const theme = ({
    colors: {
        primary,
        secundary,
        neutral,
        alerts,
    },

    buttons: {
        buttonsTheme,  
    }
})